
// ES6 syntax
import {Circle} from './circle.js'  // ideally, extension should not be mentioned but a temporary fix to the error msg. It's searching for a file circle.js 

const c = new Circle(123);
c.draw();


// Run this program in a chrome browser dev tool
