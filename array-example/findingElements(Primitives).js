
const numbers = [1, 2, 3, 1, 4];

console.log(numbers.indexOf(1)); // returns -1 if the given number is not present; otherwise index of the number

console.log(numbers.lastIndexOf(1)); // returns the last index of a given number

console.log(numbers.includes(1)); // returns true if the given number is available in an array